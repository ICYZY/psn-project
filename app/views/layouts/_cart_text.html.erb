<%= link_to content_tag(:span, "", :class => "glyphicon glyphicon-shopping-cart") + " #{total_item} Items in Cart ( #{number_to_currency(current_order.subtotal, unit: "฿")} )", cart_path, class: "btn btn-link" %>
